//Create a HangboardSessionModel model just by requiring the module
var HangboardSessionModel = require("../models/hangboard-session");

// Use the SomeModel object (model) to find all SomeModel records
HangboardSessionModel.find(callback_function);
